services:
  backend_migrate:
    container_name: backend_migrate
    build:
      context: ./backend
    networks:
      - transcendence
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    command: ["npx", "prisma", "migrate", "deploy"]
    restart: on-failure

  backend:
     container_name: backend
     build:
       context: ./backend
     networks:
       - transcendence
     environment:
       DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
       REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
       VAULT_ADDR: ${VAULT_ADDR}
       VAULT_CACERT: ${VAULT_CACERT}
       NODE_EXTRA_CA_CERTS: ${VAULT_CACERT}
       ADMIN_USERNAME: ${ADMIN_USERNAME}
       ADMIN_EMAIL: ${ADMIN_EMAIL}
       ADMIN_PASSWORD: ${ADMIN_PASSWORD}
     volumes:
       - ./vault/certs:/etc/vault/certs:ro
       - vault-approle:/vault/approle:ro
     depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
      backend_migrate:
        condition: service_completed_successfully
      vault_init:
        condition: service_completed_successfully
     healthcheck:
       test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8000/health"]
       interval: 10s
       timeout: 5s
       retries: 5
       start_period: 30s
     restart: unless-stopped

  frontend:
    container_name: frontend
    build:
      context: ./frontend
    networks:
      - transcendence
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5173"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  nginx:
    container_name: nginx
    image: owasp/modsecurity-crs:4-nginx-202606261106
    networks:
      - transcendence
    ports:
      - "443:8443"
      - "80:8080"
    volumes:
      - ./nginx/template/default.conf.template:/etc/nginx/templates/conf.d/default.conf.template
      - ./scripts/setup-certs.sh:/scripts/setup-certs.sh
      - ./nginx/certs:/etc/nginx/certs
      - ./nginx/modsecurity/before-crs.conf:/etc/modsecurity.d/owasp-crs/rules/REQUEST-900-EXCLUSION-RULES-BEFORE-CRS.conf
      - ./nginx/modsecurity/exclusions.conf:/etc/modsecurity.d/owasp-crs/rules/RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/stub_status"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    environment:
      BACKEND: ${BACKEND}
      SSL_CERT_FILE: /etc/nginx/certs/nginx.cert
      SSL_CERT_KEY_FILE: /etc/nginx/certs/nginx.key
      SSL_PORT: 8443
      PORT: 8080
      SERVER_NAME: Transcendence
      SSL_PROTOCOLS: TLSv1.2 TLSv1.3
      PARANOIA: "2"
      BLOCKING_PARANOIA: "2"
    restart: unless-stopped

  nginx_exporter:
    container_name: nginx_exporter
    image: nginx/nginx-prometheus-exporter
    networks:
      - transcendence
    command:
      - "-nginx.scrape-uri=http://nginx:8080/stub_status"
    depends_on:
      nginx:
        condition: service_healthy
    restart: unless-stopped

  vault-data-init:
    image: alpine
    command: ["chown", "-R", "100:1000", "/vault/data"]
    volumes:
      - vault-data:/vault/data
  vault:
    container_name: vault
    image: hashicorp/vault
    networks:
      - transcendence
    environment:
      VAULT_ADDR: ${VAULT_ADDR}
      VAULT_CACERT: ${VAULT_CACERT}
    volumes:
      - ./vault/config:/etc/vault/config
      - ./vault/certs:/etc/vault/certs
      - vault-data:/vault/data
    healthcheck:
      test: ["CMD-SHELL", "vault status; [ $? -ne 1 ]"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    command: vault server -config=/etc/vault/config/vault.hcl
    cap_add:
      - IPC_LOCK
    ulimits:
      memlock:
        soft: -1
        hard: -1
    depends_on:
      vault-data-init:
        condition: service_completed_successfully
    restart: unless-stopped

  vault-keys-init:
    image: alpine
    command: ["chown", "-R", "100:1000", "/vault/keys"]
    volumes:
      - vault-keys:/vault/keys

  vault-approle-init:
    image: alpine
    command: ["chown", "-R", "100:1000", "/vault/approle"]
    volumes:
      - vault-approle:/vault/approle

  vault_init:
    container_name: vault_init
    image: hashicorp/vault
    networks:
      - transcendence
    environment:
      VAULT_ADDR: ${VAULT_ADDR}
      VAULT_CACERT: ${VAULT_CACERT}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      ADMIN_API_KEY: ${ADMIN_API_KEY}
    entrypoint: ["sh", "/etc/vault/config/vault-init.sh"]
    volumes:
      - ./vault/config:/etc/vault/config
      - ./vault/certs:/etc/vault/certs
      - vault-keys:/vault/keys
      - vault-approle:/vault/approle
    depends_on:
      vault:
        condition: service_healthy
      vault-keys-init:
        condition: service_completed_successfully
      vault-approle-init:
        condition: service_completed_successfully
    restart: on-failure

  grafana:
    container_name: grafana
    image: grafana/grafana
    networks:
      - transcendence
    environment:
      GF_SECURITY_ADMIN_USER: ${GF_SECURITY_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD}
      GF_SERVER_ROOT_URL: "%(protocol)s://%(domain)s/grafana/"
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
    volumes:
      - grafana-db:/var/lib/grafana/
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      prometheus:
        condition: service_healthy
    restart: unless-stopped

  prometheus-init:
    image: alpine
    command: ["chown", "-R", "65534:65534", "/var/lib/prometheus/data"]
    volumes:
      - prometheus-data:/var/lib/prometheus/data

  prometheus:
    container_name: prometheus
    image: prom/prometheus
    networks:
      - transcendence
    volumes:
      - prometheus-data:/var/lib/prometheus/data
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./monitoring/prometheus/alert.rules.yml:/etc/prometheus/alert.rules.yml
    depends_on:
      prometheus-init:
        condition: service_completed_successfully
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/var/lib/prometheus/data"
      - "--web.external-url=https://localhost/prometheus/"
      - "--web.route-prefix=/prometheus/"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/prometheus/-/healthy"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    container_name: redis
    image: redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    networks:
      - transcendence
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis_exporter:
    container_name: redis_exporter
    image: oliver006/redis_exporter
    networks:
      - transcendence
    environment:
      REDIS_ADDR: redis:6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    container_name: postgres
    image: postgres:16
    networks:
      - transcendence
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  postgres_exporter:
    container_name: postgres_exporter
    image: prometheuscommunity/postgres-exporter
    networks:
      - transcendence
    environment:
      DATA_SOURCE_NAME: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  grafana-db:
  prometheus-data:
  redis-data:
  postgres-data:
  vault-data:
  vault-keys:
  vault-approle:

networks:
  transcendence:
    driver: bridge